{
  "firestore": [
    {
      "database": "(default)",
      "location": "eur3",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "messaging",
      "location": "eur3",
      "rules": "firestore_messaging.rules"
    }
  ],
  "storage": {
    "rules": "storage.rules"
  }
}
